{
  "name": "restaurant-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static website for CIT (イタリアン) - dev server helper",
  "scripts": {
    "dev": "live-server . --port=8000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
